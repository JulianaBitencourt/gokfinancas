import styled from "styled-components";

export const Container = styled.div`
  width: 100%;
  text-align: end;
  position: fixed;
  bottom: 0;
  left: 0;
  z-index:-5;
`;

export const Logo = styled.img`
  width: 28rem;
`;
