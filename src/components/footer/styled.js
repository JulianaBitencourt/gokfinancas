import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  justify-content: end;
  bottom: 0;
  left: 0;
  flex-direction: row;
`;

export const Logo = styled.img`
  width: 28rem;
`;
